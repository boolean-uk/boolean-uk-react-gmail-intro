import React from "react";
import InboxList from "../layout/InboxList";

const Navigation = () => {
    return (
        <nav className="left-menu">
            <InboxList />
        </nav>
    );
}

export default Navigation;